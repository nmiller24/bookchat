Let's implement the chat functionality in JavaScript:

Requirements:
1. Add these features to index.html's script section:
   - Message sending via POST request
   - Automatic message retrieval every 5 seconds
   - Message display with timestamps
   - Error handling and user feedback
   - Smooth scrolling to new messages

2. Functions needed:
   - sendMessage()
   - fetchMessages()
   - addMessage()
   - formatTimestamp()
